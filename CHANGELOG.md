v1.5.5 — LHM probe retry + diagnostics: WMI readiness check now retries up to 3 times (T+8s, T+13s, T+18s) before reporting unavailable. On failure, collects process/namespace/log diagnostics and sends them to the diag Discord channel.
