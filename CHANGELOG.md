v1.5.7

🖥️ Dashboard & Käynnistys
- Uusi kokoruudun käynnistysnäyttö — animoitu teksti, vaihekohtainen ajoitusruudukko, sujuva edistymispalkki
- Dashboard korotettu komentokeskukseksi — suuri terveyspisterengas, täysleveä Auto-Optimize-painike, Quick Health toisessa rivissä
- Quick Health -rivit täysin klikattavia — vahvistusikkuna, latausspinnerit ja tarkat vapautetut megatavut temp-siivouksessa
- HUD-sirujen ja dashboard-kipinäkuvaajien värit yhdistetty yhteen korosteväriin

✨ Maailmanluokan välilehtiuudistukset
- 🎮 FiveM — optimointipisterengas, 3-sarakkeinen korttiasettelu, värikoodatut osiot, "Käytä kaikki turvalliset" yhdellä klikkauksella
- 🔧 General Tweaks — Quick Wins -komentokeskusrengas, 13 värikoodattua osiota, reaaliaikainen käytössä-laskuri, 23 uutta infovinkki-nappia
- 🧹 Debloating — animoitu bloat-pisterengas (Puhdas→Raskas -taso), kategoriakohtaiset värirajat, historiatoiminnon kuvakemerkinnät
- 🚀 Startup — Boot Command Center -kortti livepisterenkaalla, shimmer-lataushäivytykset, uudelleenaktivoi-kaikki, hakutuloslaskuri
- ⚡ Pulse — värikoodatut käynnistyssäännöt (Update Guard / Game Shield / Game Watcher / Auto-Pulse), valitun pelin hero-banneri
- 🌐 Networking — pistesankarikori SVG-renkaalla, 3 uutta itsenäistä TCP-tweakia, yhteensä 27 tweakia (oli 24)
- 💻 Advanced CPU/GPU/Power — kiinnittyvä edistymisotsakke, 4-sirun vaiheopas, kaikki 7 osiota tiivistettävissä edistymismerkeillä
- 🏆 App Optimizer — kategoriakohtainen väri-identiteetti, hero-rengas, "Optimoi kaikki" -painike, konfliktin vaikutussirut, "Korjaa kaikki selaimet/käynnistäjät" -massiivitoiminnot

🌟 Uudet suorituskykyparannukset
- ⚡ Disable Multi-Plane Overlay (MPO) — poistaa GPU-ajurin näytön välkkymisen
- 🧠 Keep Kernel & Drivers in RAM — estää sivutusviiveet pelisäikeillä
- 💾 Large System Cache Off — optimoi RAM sovellusten käyttöön tiedostojärjestelmän sijaan
- 🔀 Split Large Caches — vähentää AMD Ryzen multi-CCX -prosessorien välimuistiviiveitä

🔬 Uudet diagnostiikkatyökalut
- 🌡️ Thermal Audit — 5 sekunnin reaaliaikainen skannaus Boost Consistency -pisteineen, kuristuksen syyn tunnistus (lämpötila / tehonrajoitus / VRM / ydinepätasapaino), ytimien kelloruudukko ja FPS-palautusarviot peleille GTA V, FiveM, Valorant, CS2 ja Apex
- 🔥 Thermal Throttle Detective — reaaliaikainen kuristus-%-aikajana min/max/avg-seurannalla
- ⚡ Power Efficiency Tracker — wattilukema dynaamisella kaavioasteikolla ja sensorilaatunauhalla
- 🕐 Latency Budget Visualizer — todelliset CPU/GPU/näyttö-vaiheiden ajoitukset PresentMonilta P99-merkinnällä

🤖 Auto-Optimize-parannukset
- Uusi vaihe 0: käyttötapavalikko — FPS Gaming / Open-World / Streaming / Mixed — valitsee automaattisesti ajotilan ja lisää käyttötapaan sopivat tweakit
- Gaming Readiness Score (0–100, animoitu) lisätty ajon jälkeiseen tulosraporttiin

🎨 Premium-viimeistely
- Otsikkorivi tiivistetty 46 pikseliin kaltevuusaksentilla; HUD yhdistetty siihen sisälle (vapauttaa 32 pikseliä)
- Kursorinseurannan korttihehku toimii nyt oikein kaikissa osiokorteissa
- What's New -ikkuna — versiohistoria-sivupalkki, automaattiset [FIX]/[NEW]/[UPDATE]-sirut
- Tweak-infopopover uudelleenrakennettu — lasiefffekti, vaaleanpunainen registry-lohko, kopioi polku -nappi
- Älykkäät hakupisteet useilla välilehdillä — tuloslasku, ✕-tyhjennä, korostusrengas
- "Käynnistä sovellus uudelleen" -muistutukset selaimen tai äänioptimointien poistamisen jälkeen
- Käynnistyksen suuren vaikutuksen varoitusbanneri ilmestyy automaattisesti, kun raskaita käynnistyssovelluksia on aktiivisena
- Advanced Mode -esittelyikkuna uudelleensuunniteltu — kuvakemerkkirivit, porrastettu animaatio, kunnollinen modaalijärjestelmä

🔨 Uudet paketoidut työkalut
- JylliJobMon.exe ja JylliSensor.exe käännetty C#-lähdekoodista ja pakattu asennusohjelmaan

🗑️ Poistettu
- ETW Timeline ja Frame DNA -ominaisuudet poistettu kokonaan

v1.5.7 — Thermal Audit world-class overhaul (8 improvements): (1) Live progress bar fills 0→100% over the 5-second scan with per-tick MHz + temp readout so the wait is informative instead of a frozen spinner. (2) Boost Consistency Score (0–100) circular gauge — measures how often the CPU hits ≥95% of its max boost clock; no tool surfaces this number. (3) Throttle Cause Fingerprinting — diagnoses Thermal / Power Limit / VRM-Board / Core Imbalance from the same data with a color-coded cause badge. (4) Per-core clock grid — one colored cell per core (green/amber/red) identifies a single sick core that tanks 1% lows. (5) Clock Timeline sparkline — 10-dot waveform shows when throttling spiked during the 5s window, not just an average; hover for exact MHz. (6) Game-specific FPS recovery — GTA V / FiveM / Valorant / CS2 / Apex estimates using per-game CPU sensitivity profiles instead of a generic number. (7) Fan RPM correlation — reads LHM Fan sensor; shows RPM in the result and flags "fans slow for temp" when RPM < 1200 at ≥75°C, sharpening the paste-vs-fan-curve diagnosis. (8) Trend vs last run — persists result to settings.thermalLastAudit and shows ▲▼ throttle% and °C deltas on subsequent scans. Handler upgraded: per-sample progress IPC events, per-core clock collection (CORES= CSV), fan RPM, boost score calculation, cause fingerprinting decision tree, game FPS profiles. New preload entries: onThermalProgress / offThermalProgress. New LANG keys (EN+FI): thermalBoostScore/Sub, thermalCores, thermalSparkline, thermalCauseLabel/Thermal/PowerLimit/Vrm/CoreImbalance, thermalFanRpm/Low, thermalGameGains, thermalTrendThrottle/Temp/New.

v1.5.7 — Fix: Thermal Audit was permanently stuck on "Running 5-second diagnostic…" — both the preload.js IPC bridge entry (pulseThermalAudit) and the main.js handler (pulse-thermal-audit) were missing, causing api.pulseThermalAudit() to silently throw on every call. Added the preload bridge method and implemented the full handler: 10 LHM WMI samples over 5 seconds (~500ms intervals), computes avgCurMhz/maxMhz/avgTempC, derives throttlePct = 100*(1−avgClk/maxClk), maps to severity (none/light/moderate/heavy at 3/10/20%), estimates FPS gain at 0.6 FPS per throttle %, and selects recommendation (thermalPaste ≥90°C, airflow ≥80°C, fanCurve otherwise). Returns the full shape the existing doThermalAudit() UI already expects.

v1.5.7 — Security fix: free users could activate Pulse from the system tray right-click menu — the tray menu called activatePulse() directly, bypassing the requiresPremium() gate that the IPC handlers use. Fixed by adding enabled:false (visually greys out the menu item) and a requiresPremium('tray-pulse') early-return in the tray click handler, consistent with all other premium IPC guards.

v1.5.7 — Added 4 new genuinely impactful tweaks: Disable Multi-Plane Overlay (MPO) — sets DWM OverlayTestMode=5, eliminating the GPU driver flicker/stutter class documented by NVIDIA and AMD; Keep Kernel + Drivers in RAM — DisablePagingExecutive=1 locks kernel and driver code in RAM, preventing paging stalls on game threads; Large System Cache Off — ensures LargeSystemCache=0 + IoPageLockLimit=0 so the kernel memory manager optimises RAM for application working sets over the file system cache; Enable Split Large Caches — SplitLargeCaches=1 enables L3 cache topology awareness in the thread scheduler, reducing cross-CCX cache-miss latency on AMD Ryzen multi-CCX CPUs. All 4 have check() functions, correct restore paths, and are wired into SEARCH_INDEX.

v1.5.7 — General Tweaks: Safe Defaults now skips pre-applied tweaks and shows a toast when all safe defaults are already active instead of opening an empty batch modal. Restore All batch now correctly clears pre-applied state from localStorage and keeps pre-applied toggles checked after restore, matching the behaviour of individual card restores.
v1.5.7 — Advanced tab tweak audit: 9 incomplete reverts fixed + 3 missing implementations added. Reverts fixed: hvci-offload now removes WasEnabledBy key; vbs-disable now also re-enables HypervisorEnforcedCodeIntegrity Enabled=1; nvidia-pstate-lock now removes PowerMizerLevel (was leaving it at 1); nvidia-profile now removes PowerMizerLevelAC and PerfLevelSrc from GPU class keys, and also removes TdrLevel/TdrDelay from GraphicsDrivers; full-mitigation-wipe now re-enables all 6 mitigations (CFG, StrictCFG, SEHOP, ForceRelocateImages, RequireInfo, NullPage) instead of only CFG+SEHOP; amd-chill-off now removes KMD_EnableAntiLag; amd-power-hold now removes DalPowerPolicy and uses individual Remove-ItemProperty calls instead of comma syntax; amd-service-trim now restores all 5 services (AMD Crash Defender, AMD External Events Utility, AMD Log Utility, amdfendr, amdxc64) instead of only 1; corsair-audio-off now also restores CorsairVBusDriver to auto. New implementations added (were in UI but silently did nothing): pool-usage-max sets PoolUsageMaximum=96 in Memory Management; wasapi-min-period sets WASAPI exclusive mode min period to 10000 (1ms) on all audio render devices via MMDevices Properties; directstorage-nvme raises NVMe QueueDepth to 64 on all NVMe controllers and enables DirectStorageEnabled+GDeflateEnabled in GraphicsDrivers.
v1.5.7 — Tweak audit: 11 incomplete/wrong reverts fixed and 6 missing tweak implementations added. Reverts fixed: visual-effects now restores FontSmoothing/FontSmoothingType/DragFullWindows/IconsOnly; mmcss now removes Clock Rate override; explorer-perf now removes LaunchTo; hpet now deletes useplatformtick BCD value; bcd-tweaks now deletes bootlog instead of incorrectly setting recoveryenabled; diag-tasks-off now re-enables all 5 disabled tasks (was only 1); diagnostics-off now removes ScenarioExecutionEnabled policy key; cortana-blackout now removes CortanaEnabled from HKCU Search; capability-ruleset now removes SchemaVersion from CapabilityAccessManager; proximity-deny now also restores bluetoothSync to Allow; msi-mode now has a real revert loop setting MSISupported=0 on all non-NIC PCI devices (was just an info message). New tweak implementations added (were in UI but silently did nothing): mmcss-proaudio sets GPU Priority=8/Scheduling Category=High on Pro Audio MMCSS task; kbdclass-queue sets KeyboardDataQueueSize=50 and ConnectMultiplePorts=0 on kbdclass driver; defender-idle-priority sets MsMpEng.exe CpuPriorityClass=5 via IFEO and adds game exclusions for CS2/Valorant/Apex/Fortnite; dpc-coalescing sets CoalescingTimerInterval=0 in kernel parameters; gpu-preemption sets EnablePreemption=1 and VsyncIdleTimeout=0 in GraphicsDrivers Scheduler; vrr-borderless sets DirectXUserGlobalSettings VRROptimizeEnable=1 in UserGpuPreferences.
v1.5.7 — Fix: Low Resource Mode toggle was a no-op — `api.setLowResourceMode?.()` silently failed because neither a `set-low-resource-mode` IPC handler nor a preload bridge method existed. Added `setLowResourceMode` to preload.js and a `set-low-resource-mode` handler in main.js. Also changed `pauseBackgroundPolling()` so that when the setting is on, instead of fully stopping background polling it throttles metrics collection to 30s intervals and context-detector (game detection) to 30s — matching the "up to 30s" description — while also clearing the thermal timer. Restoring to normal intervals on `resumeBackgroundPolling()` is unchanged.
v1.5.7 — Fix: ThermalGuard badge never populated on startup and toggle silently did nothing — `thermalGuardSet` was not exposed in preload.js (optional-chained call was a no-op), and the boot block showed the badge element but never called `api.thermalStart()` to begin polling. Added `thermalGuardSet` to preload.js (routes to existing `thermal-start`/`thermal-stop` handlers) and added `api.thermalStart()` call in the boot block when `settings.thermalGuardEnabled` is true. Badge now appears immediately on launch and correctly starts/stops when toggled in Settings.
v1.5.7 — Fix: Settings tab toggles (Discord RPC, UI Sounds, Silent Mode, Low Resource, Thermal Guard, Advanced, Motion Mode, ARIA, FPS HUD, Analytics, Start at Login) failed to persist on relaunch or quick app close — all toggle handlers were using a 400ms debounced save that was abandoned if the app closed before the timer fired; replaced with immediate api.saveSettings() calls on all discrete toggle/button handlers (debounce kept only for the accent color picker which fires continuously during drag). Added a beforeunload flush guard that also cancels any pending debounce timer and forces a final save when the window is closing.

v1.5.7 — Fix: Theme switching in Settings tab stopped working after navigating away and returning — the Settings page was being HTML-cached like static pages, which stripped all JavaScript event listeners from theme cards on every revisit; added 'settings' to _PAGE_CACHE_NO_STORE so buildSettings() always runs fresh and theme click/hover handlers are always live.

v1.5.7 — Pre-Flight Diagnostic world-class quality pass: all 10 existing features improved — no new features added. Scan bar upgraded from 2px invisible line to 6px glowing tokenized bar (var(--pink), box-shadow); progress text now shows friendly translated labels ("Scanning 70+ registry settings…") via lookup table instead of raw PowerShell step names; synthetic sub-progress interval eliminates the 60%→90% dead zone during the 45-second tweak scan (2–4pct ticks at 3.2s intervals). Scan failure renders a structured error card with a Retry button instead of a plain red div. Safety score bar and category bars both animate in via double-requestAnimationFrame (CSS transitions now actually fire on first paint). Score count-up reads --dur-long CSS variable so it respects snappy/smooth/reduced-motion modes. All hardcoded hex colors (#7a7a8a, #8a8a9a, #c0c0cc, #3498db, #a855f7, etc.) replaced with design tokens (var(--muted), var(--gray), var(--info), var(--purple), etc.) throughout Pre-Flight output. "Bonus" detection (Stock Install) filtered from the detections panel — it already appears in the score breakdown. DRY violation in doDriverScan fixed: parsWmiDate() helper replaces three verbatim copies of WMI date-parse code. Driver age thresholds corrected: >548d (18mo) = red Outdated, 365–548d = yellow Aging (was >365d red, misleadingly strict). Driver cache gets ts field with 24h TTL check to prevent stale data being consumed after a day. Driver "Download" button renamed "Find Driver" (opens manufacturer page, not a binary). Driver scan shows "Last scanned: X ago" timestamp in the header row. Aging driver count appended to summary ("· 2 aging"). Category bars use pf-cat-bar CSS class with will-change:width; rows are clickable (showPage nav); count label shows applied/total · pct%; <25% color changed from var(--info) blue to var(--muted) neutral. Pre-applied panel uses CSS tokens throughout; "Go to X" span→btn-ghost button; empty state changed from green success to neutral info card. Score breakdown adds impact% column ("−40 pts · 40%"). Detections panel icon changed fa-satellite-dish→fa-shield-halved; high-severity rows have red left-border and start expanded; others start collapsed with chevron toggle. Recommendations: low-score warning (<50) added at top; pfGoThere btn-ghost replaces inline span; entries sorted by priority field. Diff view uses pfGetFriendlyName() so badges show "Game DVR Off" not "game-dvr"; change count badge added; "0m" edge case shows "just now"; panel gets border-left:3px solid var(--info) visual weight. Benchmark: progress bar drains to 0% and hides after completion instead of snapping; baseline persists in localStorage (pf_bench_baseline) across reloads; "Current" column header fixed (was incorrectly using pfBenchRunAgain key); score units shown (prime ops/10s); delta zero guard shows ±0.0% in var(--muted) instead of green +0.0%. Action cards: separator border added above cards; DDU confirm() replaced with inline two-button confirmation (Open Page / Cancel, no browser dialog); runtime install initial label shows "Installing {n} packages…" for multi-item installs; unknown GPU falls back to pfDduUnknownGpu token. New translation keys added (EN+FI): pfStepSignatures, pfStepBoot, pfStepPower, pfStepTweaks, pfStepFinalising, pfRetry, pfGoThere, pfRecLowScore, pfDiffCount, pfDiffJustNow, pfBenchCurrent, pfBenchUnit, pfRuntimeInstallingN, pfDduConfirmInline, pfDduConfirmYes, pfDduConfirmNo, pfDduUnknownGpu, pfDriverLastScan, pfDriverAgingN.
v1.5.7 — System Restore tab deep quality pass: 7 bugs fixed + 8 UX/backend improvements. BUG: doRestoreToPoint() no longer wipes non-tweak settings (autoSnapshotDays, language, premium, etc.) on restore — only tweak_ keys are cleared. BUG: tweak diff sort() mutation fixed (spread copy before sort). BUG: restore fallback now shows a warning toast ("Opened Windows System Restore — complete the restore there") instead of a misleading red error toast. BUG: _rpParseDate() now correctly parses the WMI UTC offset field (e.g. +120=UTC+2), fixing wrong timestamps for non-UTC users. BUG: stale-snapshot banner now suppressed when auto-snapshot is disabled (no longer fires for manual-only users). BUG: "Enabling SR…" string was hardcoded English — now uses rpEnablingSR i18n key (both EN+FI). BUG: duplicate onAutoSnapshotDone IPC listeners on language switch fixed with a one-time registration guard. BACKEND: ensureSREnabled() extracted as shared helper — both manual creation and auto-snapshot now clear Group Policy block keys (DisableSR, DisableConfig) before enabling SR; manual path was previously less robust than auto. BACKEND: restore-to-point IPC now returns { fallback: true } when rstrui.exe is opened, allowing the renderer to distinguish fallback from hard failure. BACKEND: list-restore-points parse error now returns { error } instead of silently returning []; loadRPList() guards against non-array responses. BACKEND: get-sr-disk-usage now uses Get-CimInstance (modern) with Get-WmiObject fallback. UX: table refresh no longer flickers — keeps existing rows visible during reload and spins the Refresh button icon instead. UX: default description input now includes today's date (JylliTool_Manual_YYYYMMDD). UX: date column now shows a relative age hint ("2d ago", "3h ago"). UX: schedule dropdown now shows a brief "✓ Saved" fade-in confirmation after saving. UX: rpExportList() now uses the cached list (_rpLastPoints) instead of a second PS round-trip. UX: disk bar now shows Allocated GB alongside Used/Max. UX: tweak diff colors in restore confirm dialog replaced hardcoded hex (#e74c3c, #2ecc71, #aaa) with CSS vars (var(--danger), var(--success), var(--muted)). CSS: delete column right-aligned and narrowed; .rp-age-hint, .rp-refreshing, .rp-schedule-saved-tip classes added. i18n: added rpEnablingSR, rpRestoreFallback, rpScheduleSaved, rpDiskAlloc keys to both EN and FI.
v1.5.7 — i18n completion pass: all hardcoded strings in the five new features wired through the translation system. buildEtwTimeline() now uses tUI() for page title/description, filter pill labels and tooltips (DPC/ISR/GPU Frame/Core Switch), Pause-on-anomaly label and tooltip, Start/Stop button text, missing-exe card title/description/instructions, and all legend entries. etwToggle() and onEtwStopped callback use tUI('etwStartBtn'/'etwStopBtn'). loadAiAdvisor() now uses tUI() for provider chip labels (NPU/GPU/CPU), loading text, analysed timestamp, all-good card title/subtitle, intro hint, confidence labels (Strong/Good/Possible match), Apply/Applying…/✓ Applied/✗ Failed/✗ Error button states, and apply tooltip. applyAiRec() fully translated. js/frame-dna.js _fdHandleGpuError() and _fdRenderResult() now call tUI() directly (global scope) for error messages and stutter pattern descriptions with {lag}/{pct} placeholder substitution. Added missing EN+FI LANG keys: aiProviderNpu, aiProviderGpu, aiProviderCpu.
v1.5.7 — New-user UX pass on all 5 features: Frame DNA subtitle changed from "GPU autocorrelation · 512 lags · 5s dispatch" to plain-language description; waiting state now explains what to do ("Start a game and play for a few seconds…"); score ring labelled "Frame Smoothness Score (100=smooth, <60=stuttering)" with tooltip; canvas legend rewritten; stutter detection result uses "Strong/Mild recurring stutter" plain language; GPU error messages now actionable ("update graphics driver" + specific advice). Thread Map subtitle explains bottleneck detection; balance score shows "/100" scale with full tooltip explaining what low score means; Auto-Pin button text expanded to "Auto-Pin Render Thread" with tooltip. AI Advisor subtitle rewritten from "Local ONNX inference" to plain benefit text; loading message changed to "Checking your system — this takes about a second…"; provider chip gets full label tooltip; each recommendation shows "Strong/Good/Possible match" confidence label; apply button uses "Applying…/✓ Applied/✗ Failed" states; no-recs state shows a green success card. ETW Timeline: feature description added in header; DPC/ISR/FLIP/CTX filter buttons renamed to DPC/ISR/GPU Frame/Core Switch with colour-coded borders and hover tooltips explaining each term; "Freeze on anomaly" renamed "Pause on anomaly"; missing-exe banner expanded from one line to a structured card with compilation instructions; waiting canvas shows "Click Start Timeline…" placeholder; "FROZEN" renamed "PAUSED". Sensor Rate badge gains "Sensor" text label and tooltip explaining 1000Hz vs 2s poll modes.
v1.5.7 — Feature 1: ETW Kernel Timeline — added "Timeline" sidebar nav item (premium, advanced-mode) rendering a scrolling 100ms Gantt chart of Windows kernel events at 100ns resolution. New js/etw-worker.js owns an OffscreenCanvas (transferred via transferControlToOffscreen) and renders per-CPU-core lanes with DPC=orange, ISR=red, GPU Flip=purple, Ctx Switch=blue events. New js/etw-timeline.js spawns the worker, batch-flushes IPC events at 60fps, and exposes etwSetFreeze/etwSetFilter. New tools/JylliETW/JylliETW.cs is a .NET 4.8 console app using Microsoft.Diagnostics.Tracing.TraceEvent that subscribes to kernel DPC, ISR, and thread context-switch ETW providers and streams JSON-line events over \\.\pipe\JylliETW. ARIA anomaly detection now emits etw-freeze-at to the renderer when _etwRunning=true, freezing the Gantt at the spike timestamp. buildEtwTimeline() page builder handles premium gate, DPI-aware canvas sizing, filter pill checkboxes, Start/Stop button, and JylliETW.exe missing hint. Added: startEtw(), stopEtw(), etw-start/stop/status IPC handlers, etwStart/etwStop/etwStatus/onEtwEvent/onEtwFreezeAt/onEtwStopped/offEtwEvent preload bridge methods, etwToggle()/etwToggleFilter() renderer functions. New files: js/etw-timeline.js, js/etw-worker.js, tools/JylliETW/JylliETW.cs, tools/JylliETW/build.ps1.
v1.5.7 — Feature 4: ONNX Local AI Advisor — added local AI inference using onnxruntime-node (1.26.0) with DirectML execution provider that automatically routes to NPU (Intel Core Ultra / Ryzen AI) or GPU, with CPU fallback — zero cloud, zero privacy concern, zero CPU usage during gaming. loadOrtModel() tries DML first then CPU; detects NPU via CPU name string (ultra/ryzen ai). buildAdvisorFeatures() constructs a 64-float feature vector from hardware profile (CPU cores/speed, RAM, VRAM, display Hz, laptop/wifi/NVMe flags, brand), ARIA anomaly history (per-metric counts over last 1/5/10/20 sessions), PresentMon frame stats, LHM sensor values, and applied tweak bitmask. ai-get-recommendations IPC handler runs inference, filters scores > 0.55 for unapplied tweaks, returns top-5 ranked recommendations. AI Advisor card on Dashboard shows provider chip (NPU/GPU/CPU colored badge), confidence progress bars, and one-click Apply buttons. Card hidden when model file absent (assets/ai/jylli-advisor.onnx not yet committed). Added: loadOrtModel(), buildAdvisorFeatures(), TWEAK_CANONICAL constant, ai-get-recommendations and ai-get-status IPC handlers, aiGetRecommendations/aiGetStatus preload bridge methods, loadAiAdvisor()/applyAiRec() renderer functions. Dependencies: onnxruntime-node added; asarUnpack expanded to include node_modules/onnxruntime-node/.
v1.5.7 — Feature 2: WebGPU Frame Coherence Analyzer ("Frame DNA") — added a WebGPU compute pipeline that runs normalized Pearson autocorrelation across 512 lag windows entirely on the GPU, detecting periodic stutter cadence (e.g. "every 16 frames = driver buffer swap") that simple percentile stats miss. PresentMon stdout handler now also writes parsed MsBetweenPresents into a SharedArrayBuffer ring-buffer (4096 slots); COOP/COEP headers injected at createWindow() to enable SharedArrayBuffer in the renderer. New js/frame-dna-worker.js owns the WebGPU device, runs a WGSL @compute workgroup_size(64) shader every 5s, reads back 512 autocorrelation floats, computes a Frame Regularity Score (0–100), and posts results to the main thread. New js/frame-dna.js orchestrates the worker and renders the indigo→cyan spectrum chart + dominant-lag orange dashed marker on a Canvas 2D element in the Pulse panel. Score ring updates color (green >80, amber 60–80, red <60). GPU badge flashes on each dispatch. Panel always visible in Pulse; graceful fallback when navigator.gpu is null. Added: get-frame-sab IPC handler, getFrameSab/offFrameDnaTick preload bridge methods, attachFrameDnaCanvas()/initFrameDna()/destroyFrameDna() renderer functions. New files: js/frame-dna.js, js/frame-dna-worker.js.
v1.5.7 — Feature 5: Multi-Core Thread Mapper — added JylliJobMon.exe C# tool (tools/JylliJobMon/) that uses NtQuerySystemInformation to capture per-thread CPU affinity at 100ms resolution and streams binary frames over a named pipe. Electron main process writes thread data into a SharedArrayBuffer; a Web Worker pool (one worker per logical CPU core via navigator.hardwareConcurrency) reads per-core slots from the SAB and feeds a WebGL2-style Canvas heatmap (deep purple→cyan gradient). Thread Map panel auto-shows in the Pulse tab when a game is detected and hides on exit. Auto-Pin Render Thread button calls existing cpu-pin-game IPC. Added: startThreadMapper(), stopThreadMapper(), get-thread-sab, start-thread-mapper, stop-thread-mapper IPC handlers, getThreadSab/startThreadMapper/stopThreadMapper/onThreadMapTick/onThreadMapNames/offThreadMapTick preload bridge methods. New files: js/thread-mapper.js, js/thread-lane-worker.js.
v1.5.7 — Feature 3: 1000Hz LHM Named-Pipe Sensor Bridge — added JylliSensor C# tool (tools/JylliSensor/) that hosts LibreHardwareMonitorLib.dll in-process and streams binary sensor frames at 1000Hz over a Windows named pipe. Electron main process connects via net.Socket, decimates to 250Hz, and forwards as sensor-tick events. CPU/GPU temp and GPU usage in collectMetrics() now prefer the high-rate sensor values over the WMI 2s poll when the bridge is active. ARIA anomaly detection latency drops from 2000ms to 4ms. Pulse tab shows a live "NNNHz" green badge (or "2s poll" amber fallback). Added: startSensorBridge(), stopSensorBridge(), sensor-get-schema, sensor-get-rate, sensor-bridge-status IPC handlers, onSensorRate/onSensorTick/offSensorTick preload bridge methods. asarUnpack updated for sensor/jobmon/etw/ai asset dirs.
v1.5.7 — Fix: FPS HUD size buttons silently applied medium for all selections and never persisted — set-fps-hud-size handler had mismatched key map (small/medium/large vs sm/md/lg/xl sent by the UI), xl had no entry, and the handler never called saveSettings; fixed key map to sm/md/lg/xl with xl dimensions added, and size now saved to ariaFpsHudSize matching the color/corner persistence pattern.
v1.5.7 — Fix: premium content leak on all premium tabs — Game Optimizations, Hardware, Pulse, Scheduler, Latency Monitor, and BIOS Tuner were rendering their full UI before appending the lock overlay, making content visible through the blur scrim to non-premium users. All six builder functions now check isPremium at the very top and return immediately (showing only the lock banner on a blank page) before any content is rendered, matching the existing FiveM pattern.
v1.5.7 — Security fix: closed HMAC bypass that allowed free users to access all FiveM premium tweaks. Root cause: _settingsHmac() returned '' when hmacSecret was absent, and requiresPremium() compared '' === '' which evaluated hmacOk=true — any settings file with isPremium:true but no hmacSecret passed all premium gates. Fixed by returning 'no-secret' from _settingsHmac when the secret is missing, and adding an explicit !hmacSecret early-return in requiresPremium(). Also fixed the offline boot fallback in boot-logic.js that read cached isPremium directly without HMAC validation — now calls get-cached-tier IPC which applies the full hmacSecret+expiry+HMAC check before granting cached premium.
v1.5.7 — Fix: FiveM tweaks now blocked at the frontend for free users with a toast — previously applyTweak() had no premium guard for fivem-* IDs, so a free user who reached applyTweak via fivem-auto-opti (or any other path) would get a silent backend rejection with no UI feedback. Added the frontend check to match main.js's requiresPremium guard.
v1.5.7 — Full diagnostic fix pass (20 issues): C1 — hardcoded dev debug path (C:\Users\Pete\admin-debug.txt) moved to userData dir. C2 — setFpsHudColor, setFpsHudCorner, closeFpsHudSettings IPC calls silently failed because neither preload.js entries nor main.js handlers existed; all three added end-to-end. H1 — auth-verify catch block referenced `s` before it was assigned; moved declaration to let-before-try so offline premium users aren't locked out when loadSettings throws. H2 — relaunch-admin execFile error was ignored; now checks callback error and only quits on success. H3 — fivemRunningCheck IPC fully wired; warning banner in fivem-settings now correctly shows when FiveM is running. H4 — onPulseSessionEnd added to preload.js; pulse session debrief now fires. H5/M1 — offAriaSessionSummary, offPulseTick, offLivePingTick, offGameWatcherEvent, offAriaTick all added to preload.js; previously these listeners had no cleanup path and stacked on every page re-enter. M2 — _thermalTimer, _effTimer, _latTimer, _ariaTimer, _ariaFpsTimer now cleared in mainWindow closed handler instead of spinning until process exit. M3 — window.stopFivemIntervals defined in fivem-logic.js; index.html was already calling it but the function never existed, so bookmarks interval was never cleared on tab switch. M4 — fivemBookmarksRender re-checks DOM existence after await api.listGameBundles() so navigating away mid-fetch no longer crashes silently. M5/M6 — dead ipcMain.handle calls for discord-op-start/step/end and analytics-set-last-action removed (no preload exposure, never callable). M7 — redundant _saving=false reset in fivem-settings save error path removed (already reset by finally block). M8 — z-index layering in fivem-settings aligned to token system: tooltip 9999→7000, export-menu 100→6500, toast 999→8500. M9 — 5s startup setTimeout now guards mainWindow existence before running createRestorePointInternal to avoid holding the process alive after early close. M10 — navigator.clipboard.writeText in fivem-logic.js and fivem-settings.html now has .catch() with user-facing error toast. L1 — deleteProfile now resets select.value after populateProfileSelect so stale selection no longer silently loads a deleted profile. L2 — lock screen cancelBtn and btn listeners removed before lock screen teardown to prevent accumulation across re-shows. L3 — fps-hud-settings loadSettings .then() guarded with _alive flag; if window is closed before promise resolves the handler exits without touching the DOM. L4 — tooltip repositioning in fivem-settings replaced hardcoded 248px with dynamic offsetWidth/getComputedStyle lookup for correct DPI scaling.
v1.5.7 — Fix: free users briefly saw premium features after Discord link — auth-poll was trusting the isPremium field from the poll response directly, which could be stale or incorrect; now performs an immediate /auth/verify call using the freshly-issued token and uses that authoritative result before calling init(), so the app always opens with the correct tier.
v1.5.7 — Premium security hardening: closed 5 bypass gaps — (1) _settingsHmac fallback key removed: missing hmacSecret now returns an invalid HMAC instead of falling back to the known hardcoded string 'fallback', blocking forged settings-file attacks; (2) run-tweak IPC now guards fivem-* prefixed tweaks with requiresPremium() — free users who called these directly from DevTools bypassed the auto-opti filter entirely; (3) auth-verify offline catch now validates HMAC before returning isPremium:true — editing the settings file and killing the network no longer grants premium; (4) all 20 BIOS Tuner IPC handlers (get-bios-info, bios-scewin-read/write, bios-*-profile, bios-hyper-v, bios-cstates, bios-intel-power, bios-ram-stress, bios-lhm-sensors, bios-boot-chronicle, bios-stability-check, bios-capsule-snapshot/check, bios-get-change-log, bios-clear-change-log) now require premium — the UI gate existed but the IPC layer was open; (5) toggle-fps-hud and open-fps-hud-settings now require premium — adjacent aria-start was guarded but these two weren't.
v1.5.7 — Deep-scan round 2 bug fixes: 10 incomplete tweak reverts fixed — telemetry restore now also removes AllowTelemetry policy key and restores dmwappushservice; mmcss restore now reverts SFIO Priority back to "Normal" (was missing); ntfs restore now re-enables encryptpagingfile (Windows default 1); cortana restore now removes AllowCortana policy key (was only restarting WSearch service); priority-sep restore now reverts SystemResponsiveness to 20 (was only reverting Win32PrioritySeparation); disable-fso restore now removes GameDVR_FSEBehaviorMode (was only removing DXGIHonorFSE); cstates restore now resets IDLEPROMOTE and IDLEDEMOTE to 50 each (Windows Balanced defaults); fivem-network restore now also removes DefaultTTL and MaxUserPort (was only removing 4 of 6 modified keys); fivem-mmcss restore now reverts GPU Priority to 2, Scheduling Category to Medium, and SFIO Priority to Normal (was only reverting 2 of 5 keys); fivem-fso restore now covers all 3 executables including FiveM_b3095_GTAProcess.exe (was only 2). Pulse stuck-button fixes: doPulseStart now has try/catch so button re-enables if the IPC call throws; doPulseStop now has try/finally so the UI always returns to the start state even on failure. Hardware hardening: RyzenAdj handler now checks CPU manufacturer via WMI and returns not_amd error on non-AMD systems (prevents silent failure on Intel); parse() fallback changed from '0' to null with output-length guard so read_failed is returned when ryzenadj produces no output. LHM startup signal: startLhm() now sends lhm-status IPC events when the exe is missing (not_found), when the process spawn fails (spawn_error), or when elevation is denied (permission_denied) so the renderer can surface a user-facing message instead of silently showing empty charts. fivem-settings save button: fivemWriteSettings await wrapped in try/catch/finally so btn always re-enables even when the IPC call throws.
v1.5.7 — Deep-scan bug fixes: 6 incomplete tweak reverts fixed — game-dvr now restores all 5 modified registry keys (was only 1), visual-effects restore now reverts TaskbarAnimations (was only VisualFXSetting), win-tips restore now resets all 3 ContentDelivery keys (was missing SubscribedContent-310093Enabled), fivem-priority restore now reverts CitizenFX.exe (was missing), fivem-io-priority restore now reverts FiveM_b3095_GTAProcess.exe (was missing), fivem-vm restore now resets LargeSystemCache (was only DisablePagingExecutive). UI crash fixes: doGameBoost wrapped in try/finally so button never stays stuck; submitBugReport wrapped in try/finally so button re-enables on IPC failure; runWindowsHealth wizard step guarded with try/catch and r.issues null-check to prevent crash; getSystemInfo wizard step guarded with try/catch and sysInfo null-fallback; pfToggleFreeze wrapped in try/catch with error toast. Event listener stacking fixed: 14 on* calls in init() (onAutoOptiProgress, onDebloatProgress, onAriaTick, onTierUpdate, onGameWatcherEvent, onUpdateAvailable, onUpdateProgress, onUpdateDownloaded, onGameDetected, onGameExited, onPulseTick, onTrayPulseChanged, onPingResultV2) and onAriaSessionSummary in buildFiveM() now each have a matching off* call immediately before registration, preventing listener accumulation on repeated init. Translation gaps closed: 'Failed to update startup task', restore point error, 'Boosting…'/'Boost Active' button labels, 'No lines to show', 'X known pattern(s) detected', 'Test failed — check your internet connection' all wired through tUI()/tWizard() with full EN+FI keys added.
v1.5.7 — Runtime hardening: Process Freeze Add-Type guard prevents recompile-per-call and class-already-defined silent failures (freeze + defrost both patched). CPU Topology hybrid detection broadened to cover i3/gen14–16/N-series/Lunar Lake/Panther Lake; P/E core split now uses Win11 CimInstance NumberOfEfficientCores with ratio fallback. IRQ Device list registry recurse scoped to PCI hive only (was full Enum hive — could time out). BIOS Boot Chronicle now detects disabled Diagnostics-Performance/Operational log and returns logDisabled:true instead of silent empty array. BIOS Capsule Check regex broadened to accept "Current Value=" (non-AMI vendors) alongside "Selected Value="; scewinMissing flag added to response. Runtime Installer now returns missing:true per item when installer files are absent (assets/runtimes/ directory created with .gitkeep).
v1.5.7 — Fix: Scheduler Run Now, Enable/Disable, and History buttons all silently did nothing — run-scheduled-tweak, toggle-scheduled-tweak, and get-scheduled-task-history IPC handlers were completely missing from both main.js and preload.js. Added all three: Run Now calls Start-ScheduledTask, toggle calls Enable/Disable-ScheduledTask, history reads the Microsoft-Windows-TaskScheduler/Operational event log filtered to the task name.
v1.5.7 — Fix: Scheduler "Active Scheduled Tasks" stuck on Loading — the list-scheduled-tweaks IPC handler had no try/catch around runPS, so a PS queue timeout or execution error caused the handler promise to reject; the renderer's loadScheduledTasks had no catch either, so any rejection left the "Loading…" div in place forever. Both layers now catch and fall back to an empty array, which renders the proper empty-state message.
v1.5.7 — Scheduler tab quality pass: Fixed Finnish locale gap — "Yesterday", "Just now", all countdown strings ("in Xm/Xh Xm/Xd"), and the empty-state message now go through the translation system instead of being hardcoded English. State badge colours moved from inline styles to CSS classes (sched-badge-ready/running/disabled/unknown). Disabled task cards visually dim to 0.6 opacity and suppress the hover border highlight. "+ Schedule" button is disabled while the API call runs, preventing double-submits. "Run Now" button disables during the run and refreshes when complete (replaces the blind 1200ms setTimeout). History button locks while fetching run history to prevent race clicks. Bulk Enable/Disable/Delete bar disables during the operation. Countdown tick skips the DOM write when the value hasn't changed. _schedEsc helper hoisted out of the per-card render loop. History timestamps use a consistent short date+time format via toLocaleString options.
v1.5.7 — Fix: Latency Budget Visualizer now shows real per-stage timing from PresentMon instead of fake fixed ratios — parses MsInPresentAPI (CPU), MsUntilRenderComplete (GPU), and MsUntilDisplayed (display) columns directly from _pmFrameBuffer. CPU/GPU/Display bars and bottleneck detection now reflect actual measured values. Falls back to hiding the bars gracefully when stage columns are unavailable (e.g. dropped frames).
v1.5.7 — Fix: Thermal Throttle Detective, Power Efficiency Tracker, and Latency Budget Visualizer now work — all three features had complete frontends but were missing IPC handlers in main.js and bridge methods in preload.js. Added thermal-start/stop (polls LHM WMI every 2s for CPU temp, load, and clock frequency to detect thermal vs power-limit throttle), efficiency-start/stop (polls nvidia-smi for NVIDIA power draw, falls back to LHM Power sensors for CPU+GPU combined watts every 3s), and latency-budget-start/stop (reads _pmFrameBuffer from the PresentMon session, computes avg/P99 frame time and CPU/GPU/display breakdown every 2s). Also added 8 missing preload.js bridge entries: thermalStart, thermalStop, efficiencyStart, efficiencyStop, onEfficiencyTick, latencyBudgetStart, latencyBudgetStop, onLatencyBudgetTick.
v1.5.7 — Settings tab comprehensive improvement: Fixed two silent IPC crashes — ThermalGuard and Low Resource Mode toggles called non-existent api methods that blocked the sidebar badge from updating; both guarded with optional chaining. applyTheme gains an `animate` flag — hover preview and restore on theme cards no longer trigger the 60ms body opacity flicker (flicker is now reserved for committed clicks only). Motion mode buttons and toggle pill dots replaced hardcoded `#121214` with `var(--bg)` so both stay legible against any custom accent color. Discord Unlink now shows a confirmation dialog before logging out (was instantly destructive). Three toggle toasts (Analytics, ThermalGuard, Discord RPC) fixed to use the i18n system — Finnish users no longer see English text. "Check for Updates" button text and spinner label now fully translated EN+FI. Health check result area fades in smoothly (was abrupt display:block) and stale results clear before each new check. Export Report section no longer shows "Export System Report" twice — the inner row now reads "Save a full system snapshot". All UI text changes fully translated EN+FI.
v1.5.7 — Game Profiles tab comprehensive improvement: Scan button debounced (double-click guard with `_gpScanRunning` lock); button always restores correctly with icon after scan completes or fails. `populateGameProfileSelect` fixed async race condition — re-queries the DOM after await so navigating away mid-load no longer writes to a stale element; shows "Loading…" skeleton while fetching. `doGPApply` error messages now typed: distinguishes admin/access errors, path-not-found, and generic failures. Ready card upgraded with semantic `.gp-ready-dot` (10px, pulsing green keyframe when profile active, smooth color transition). "What This Applies" list replaced with color-coded category icon badges (perf/audio/gfx/sec/sys). Detail panel slides in with `gpDetailIn` animation on each game selection. Game list items gain `tabindex`, `role="button"`, and `onkeydown` for full keyboard navigation. Empty-state card shown when search finds no results (icon + contextual message). Bundle cards get `.gp-bundle-card` class with hover lift, Activate button shows spinner during async activation, empty bundle state upgraded to dashed-border card with hint text. `gpActivateBundle` now validates assigned profile still exists before activating (warns if missing). Watcher strip uses semantic class-based `.gp-watcher-dot` (animated when running, smooth transition on toggle); description dynamically changes to "Watching — tweaks apply when a game launches, restore on exit" while active; Auto-Pulse shown as a proper pill badge. CSS additions: press/scale state on list items, focus-visible ring, emoji scale-on-hover, detail-icon lift-on-hover, ready card color transition, tip card hover lift, status badge overflow protection, all using design-token easing/duration variables.
v1.5.7 — Startups tab comprehensive improvement: Fixed critical double-launch bug where using the Delay feature left the original registry entry intact, causing the app to launch twice at every boot (original entry is now removed after the Scheduled Task is registered). Task Scheduler list no longer polluted with 30–50 deep Windows system tasks under \Microsoft\ — they are filtered out, showing only user-relevant tasks. Boot impact event window increased from 150 to 300 events for better coverage. Toggle switch now performs an in-place DOM update (no full page reload, no flicker, no search state reset). Summary bar (total/enabled/disabled/high-active counts) updates instantly on toggle without a fetch. Recommended button now shows a confirmation dialog with an exact count before bulk-disabling anything, and fires all IPC calls in parallel then reloads once (was firing N individual reloads). Search filter correctly hides the entire Disabled section when no rows inside match, skips \<summary\> misidentification, and shows a "No matching startup items" message on zero results. Path abbreviation: long command paths now display as …\parent\filename.exe with the full path on hover. Impact badge now upgrades from Low/Medium to High/Medium when actual Windows boot-impact data (Event ID 101) shows the item was slow — prevents misleading Low badges on items that actually delay login. Open Location button now correctly handles unquoted paths with spaces (e.g. C:\Program Files\…). EN+FI translations added for confirmation dialog and no-results message.
v1.5.7 — Advanced Mode intro popup redesigned: replaced inline-style card with the app's modal system (.modal-overlay/.modal classes, spring entrance + animated exit). Features now shown as icon-badge rows with staggered slide-in animation. BIOS Tuner badge is amber to signal its risk before the warning callout. Warning block uses the .tip.warning component. "Don't show again" checkbox persists to settings.advIntroSeen. Post-close sidebar pulse draws attention to the 3 newly unlocked nav items. Escape key dismisses the popup. All content translated EN+FI.
v1.5.7 — Advanced CPU/GPU & Power tab comprehensive polish: Search upgraded with synonym map (searching "lag", "stutter", "heat", "interrupt" etc. now finds relevant tweaks), clear button (×), Escape-to-clear, and a "no results" empty state. Restore Point banner now shows confirmed green state after creation and can be permanently dismissed. IRQ Sculptor: richer before/after status messages with core counts, device list rebuilt as scannable 2-line mini-cards with "+N more" chip, reboot badge properly translated, Restore spinner now always visible ≥600ms. Power Efficiency Tracker: watt reading persists after Stop (shown greyed as "last session"), chart scale is now dynamic (auto-adjusts to peak seen), sensor quality shown as colour-coded badge with accuracy note for non-NVIDIA sensors, context line replaced with rolling sample count. Thermal Throttle Detective: timeline bars widened from 3px to 6px and container taller (48px), min/max/avg temperature tracked and shown in status line, legend always visible before monitoring starts, CTA instructions stay visible (dimmed) during monitoring. Latency Budget Visualizer: PresentMon error now includes a Download link, P99 shown as an amber badge distinct from avg, last bottleneck result and frame count retained after Stop, bars taller (72px) and wider (40px) with ms labels above bars. CPU Topology Optimizer: found-games count shown in header, persistent "Currently pinned" chip appears on load and updates on pin/unpin. Badge font increased from 9px to 10px with white-space:nowrap to prevent Finnish label clipping. "Clean up old power plans" button given amber colour and border for visual weight. All changes fully translated EN+FI.
v1.5.7 — Advanced tab quality pass: Process Manager — all user-facing strings now use the translation system (EN+FI) for freeze/defrost/kill toasts, process count, session savings, and hog rail labels; sort column indicators (active column highlighted, arrow flips direction on toggle); error handling in loadProcesses shows a localised message in the table on API failure; game-boost refresh no longer waits 1s unnecessarily. Latency Monitor — status card ("Excellent/Moderate/High") and sub-text now translated; DPC spike log entries show CRITICAL/HIGH/MED severity badge; DPC and ISR charts now draw a dashed 3% danger threshold line and y-axis scale labels (0%/mid/max) for at-a-glance reading; spike log exports as CSV (with Severity column) instead of plain text. BIOS Tuner — RAM stress test now shows a live countdown ("Ns remaining") while running and result messages are translated; Recovery Guide now starts expanded so first-time users see it immediately; danger banner gains an "I understand the risks — proceed" button that collapses it to a slim green acknowledged bar for the session; Change History panel gains a "Revert All" button (orange, confirmation dialog) that applies all logged changes in reverse order via SCEWIN.
v1.5.7 — Networking tab quality pass: Fixed 20 bugs across state detection, restore logic, and diagnostic tools. State detection overhaul: tcp-stack was checking the wrong registry key entirely (QoS key instead of TCP globals) — now reads `netsh int tcp show global` and checks autotuninglevel, timestamps, and maxsynretransmissions. Added 8 missing state detections that were completely absent: tcp-keepalive, dns-doh, disable-autoproxy, nic-rss-affinity, nic-ndis-poll, dns-quad9, dns-adguard, dns-custom. Fixed disable-netbios hardcoded to `$false` — now checks the actual NetbiosOptions registry key. Added dns-doh detection via `Get-DnsClientDohServerAddress`. nic-energy-efficient detection broadened from `*EEE*` keyword to also match "Green Ethernet" and "Ultra Low Power Mode" variants. tcp-ctcp detection upgraded to case-insensitive `-imatch`. Restore fixes: tcp-stack restore was missing 4 commands (ecncapability, timestamps, maxsynretransmissions, initialrto) — all now correctly restored to Windows defaults. tcp-buffers restore was missing Tcp1323Opts, SackOpts, DefaultTTL from the remove list. tcp-retransmit restore now sets the default value of 5 (was removing the property — functionally the same but explicit). nic-rss-affinity restore now also resets MaxProcessors to 0. nic-flow-control restore now tries "Rx & Tx Enabled", "Tx and Rx Enabled", and "Enabled" value variants for broader NIC compatibility. winsock-reset now writes its log to %TEMP%\ip-reset.log instead of the current working directory. DNS Benchmark rewrite: the previous script had a fatal logic bug where `$times.Add()` was never called on the TCP success path and `$sw2` was unreachable dead code — every provider silently fell through to the ICMP fallback. Replaced with a clean TCP port-53 connect-time measurement (proper Stopwatch per sample, ICMP as fallback only when <2 TCP samples collected). Live Ping Monitor: each tick now sends 3 pings and reports the median instead of a single noisy sample. Tick interval adjusted from 2s to 3s. Net Health Check DNS test: added a throwaway warmup query before the 5 timed measurements to eliminate cold DNS cache penalty on the first sample.
v1.5.7 — Debloating tab: all existing features fully operational — 13 previously-dead API calls now wired end-to-end (scan-provisioned-uwp, get-service-statuses, enable-service, get-task-states, enable-scheduled-task, get-ads-states, kill-all-ads, set-ads-toggle, get-shell-extensions, remove-shell-extension, get-optional-features, disable-optional-feature, get-user-assist). Services tab now shows live Running/Stopped/Disabled badges from the OS (single batched PowerShell call) and re-enable from history restores the exact original start type instead of always defaulting to "demand". Scheduled Tasks tab state badges (✓ off / ● on) now reflect real Task Scheduler state and use data-state attributes instead of brittle text matching, making re-enable reliable. Windows Ads/Suggestions panel now reads current registry state and applies per-toggle or all-at-once changes. Shell Extensions scanner now enumerates approved context-menu handlers with DLL existence checks and removes orphaned entries. Optional Features (DISM) scanner now lists and disables legacy Windows components with restart detection. Win32 apps UserAssist "Xmo ago" usage badges now populated from UserAssist registry (ROT13-decoded, FILETIME-parsed). UWP removal optimised from one PowerShell spawn per package to a single batched script — saves ~300ms per app removed. After UWP removal the install-status badges rescan automatically instead of staying stale. Bloat score now uses actual OS service state (badge class) instead of checkbox state for accuracy. Score history now debounced (1s) so rapidly clicking preset buttons no longer hammers localStorage. UserAssist display name matching tightened to prevent false positives on short or partial name overlaps. Progress bar listener registered once globally instead of stacking on every Win32 uninstall click.
v1.5.7 — General Tweaks tab quality pass: Fixed Quick Wins strip and "Apply All" button not reflecting state of the 4 newest entries (mmcss-proaudio, kbdclass-queue, defender-idle-priority, vrr-borderless) — both genRefreshQuickWinStrip and genApplyQuickWins were still referencing the original 6-item list after the strip was extended to 10. Added 23 new info-button tip entries (registry paths + EN/FI impact descriptions) bringing GEN_TIP_DATA coverage from 23 to 46 tweaks — every Safe Defaults tweak, every Quick Win, and every reboot-required tweak now has a tip. Improved 16 tweak descriptions with specific performance metrics and plain-language benefit language (e.g. "Sets Win32PrioritySeparation=38 — active window receives 3× longer CPU quanta", "Measured FPS gain: 2–10% on older Intel CPUs", "20–50% lower interrupt latency in LatencyMon"). Search filter debounce increased from 60ms to 150ms; added a "No tweaks match '…'" empty-state message when search returns no results (previously sections silently disappeared). Toolbar search upgraded: now uses the design-system search-bar-input CSS class for proper focus ring, and gained a one-click × clear button that appears when there is text. Batch progress modal auto-dismiss extended from 800ms to 2000ms so the results log is actually readable; a Close button now appears immediately when the batch completes so users can dismiss early without waiting. Reboot bar now shows the names of the pending tweaks in a subtitle next to the count (e.g. "Disable HPET, GPU Hardware Scheduling…"), truncated at 60 chars. Applied/Restored toast messages now look up the real display name from _genSections instead of reconstructing it from the kebab-cased ID (was showing "Game Dvr" instead of "Disable Game DVR / Xbox Game Bar"). Tip popover registry path block gained a copy-to-clipboard button (✓ confirmation, reverts after 1.2s) for power users who want to verify the key in regedit. Section progress badges now show a ✓ checkmark icon when all tweaks in a section are applied, in addition to the existing green color.
v1.5.7 — FiveM tab deep quality pass: HUD auto-toggle now reverts the checkbox and shows an error toast if settings save fails (was silent). HUD ping loop replaced with a lightweight TCP connect probe (no PowerShell spawn every 3s — 20× less overhead). CitizenFX.ini batch write now creates an atomic .bak backup before writing (matches the safety pattern of gta5_settings.xml). Mod Conflict Detector cache note replaced with a relative timestamp ("cached 12s ago") — was confusing "(cached)" text. Session history duration formatter handles sub-minute sessions (shows "45s" instead of "0 min"). CFX Health DNS tip now only fires when 2+ hosts are slow, not any single high-latency host — prevents false positives for non-US users. Log Classifier empty-state now correctly says "launch FiveM first" instead of showing "no errors found" when the log was never loaded. Smart Cache Manager error message is now translated (EN+FI) and actionable. CFX Health pinging status string now uses the translation system. FiveM process detection for cache clear upgraded from a hardcoded name list to a wildcard pattern that covers future FiveM build names. Bookmark connect now shows a warning toast if a linked profile bundle fails to load (was silent). Copy Report uses the current language for all field labels. Performance budget bar in Graphics Settings now animates from 0 on first load instead of jumping to value instantly. XML export in Graphics Settings is now correctly labelled as a partial changes snippet, not a full file.
v1.5.7 — Fix: Disk Health Timeline always-spinning — api.getCleanupHistory and api.getDiskProjection were missing from preload.js and had no IPC handlers in main.js, causing a synchronous TypeError inside Promise.all that prevented the spinner from ever clearing. Added get-cleanup-history, add-cleanup-history-entry, and get-disk-projection IPC handlers (persistent JSON at jt_cleanup_history.json in userData; disk projection via Get-PSDrive C); wired all three in preload.js; added existence guards in the renderer Promise.all.
v1.5.7 — UI fix: Cleanup tab bottom spacing — added margin-bottom to the Disk Health Timeline card so it no longer appears glued to the info tip banner below it.
v1.5.7 — Fix: Quick Clean modal no longer gets stuck open after all tasks finish — each task now has a 15-second timeout so a hanging IPC call is treated as a failure and the loop continues; overlay also gains click-to-dismiss (click the dark background to close early).
v1.5.7 — Fix: Restore Point failure message no longer dumps the full PowerShell command string into the log — when stderr is empty and Node reports a generic "Command failed:" error, the log now shows "Exit code N" instead of the entire command invocation.
v1.5.7 — App Optimizer deep polish: Fixed broken indeterminate animation on progress bars (.ao-prog-bar.ind and .tc-bar.ind were referencing a non-existent @keyframes indeterminate — now use the correct tweakShimmer shimmer). Added border-radius:1px to .ao-prog-bar fill. .ao-badge DEFAULT state contrast improved (was near-invisible muted-on-muted; now uses c-text-secondary with a subtle border). .ao-bullets li font size raised from 9px to 10.5px. All LANG conflict strings capitalized (were "conflict detected…" — now "Conflict detected…") in both EN and FI. Added aoEnabled/aoDisabled/aoAlsoConsider/aoFixAllBrowsers/aoFixAllLaunchers translation keys (EN + FI). Module-scope _aoInstalled and _aoRunningUnoptimized vars added so helper functions can reference current state without re-scanning. SGM ACTIVE badge gets id="sgm-active-badge" and toggles reactively on click without page rebuild. sgmToggle toast now shows the specific feature name (Update Intercept / Idle Suppression) plus "Enabled"/"Disabled" instead of the redundant "Smart Gaming Mode / Enabled" pair. Removed redundant runningChip from app cards (LIVE badge in header already communicates running state). Apply All Running bar now truncates app name list at 3 names with "+N more" and has overflow protection. Conflict banner now adds "Fix All Browsers" and "Fix All Launchers" buttons for the multi-app browser-gpu-pressure and launcher-trio-unapplied conflicts. Conflict re-evaluation after fix: aoConflictFix now calls _aoRebuildConflictBanner() which re-checks all four conflict conditions live and removes only resolved rows, rather than blindly removing the row and leaving stale state. gpu-queue-double cross-nudge: after fixing one side (Discord), if Chrome is still unoptimized a delayed toast suggests optimizing it too. New aoConflictFixAll() function for bulk browser/launcher fixes. doAppOpti badge during load is now a spinner icon instead of plain "…". Error path badge uses ao-specific keys (aoApplied/aoDefault) instead of stale badgeText(). Progress bar completion now fades out smoothly (fills to 100% over 0.4s, then fades opacity to 0 over 0.6s, then resets) instead of abrupt 2s delay-then-snap. Restore action badge now shows DEFAULT with rotate-left icon instead of "Restored" from the history tab. Fingerprint banner insertBefore now anchors to conflict banner or first section-card — was falling back to c.children[1] (the SGM panel). Fingerprint Restore button now uses tUI('restore') (was hardcoded English "Restore"). Fingerprint cause-text now shows a spinner during the async drift-cause lookup. _bbUpdatePanel now uses tAOName() for proper display names instead of raw slug stripping; adds slot key diffing to avoid full DOM re-render on every tick (only rebuilds when slot set changes); slot rows gain min-height and padding for readability; OVER warning uses FA triangle-exclamation icon instead of ⚠ emoji. _bbShowSessionReport now renders a flex-column per-app list with stats right-aligned instead of a single pipe-joined line.
v1.5.7 — Analytics embeds enriched: all 5 event types posted to the analytics Discord channel now include OS Build, disk type (NVMe/SSD/HDD), system uptime, and GPU. crash events add last-action context, uptime-at-crash, and recurring crash counter. session_end events add the list of applied tweak IDs and average time per tweak. bug_report events add session number, lifetime tweak count, and FiveM status. session_start events include CPU/GPU temps when LHM data is available. A shared hwFields() helper was introduced in bot.js to reduce duplication across all embeds.
v1.5.7 — Fix: Pulse page crash "api.pulsePrearmScan is not a function" — added pulsePrearmScan and pulsePrearmDefer bridge entries to preload.js (wired to pulse-prearm-scan and pulse-prearm-defer IPC channels); both callers already guard with .catch so the feature degrades gracefully until main.js implements the handlers.
v1.5.7 — Fix: App Optimizer page crash "api.onBudgetTick is not a function" — added onBudgetTick and onBudgetSessionReport bridge entries to preload.js (wired to budget-tick and budget-session-report IPC channels); guarded both calls in index.html with typeof checks for defensive safety.
v1.5.7 — Auto-Optimize upgraded: new Step 0 use-case selector (FPS Gaming / Open-World / Streaming / Mixed) added as the first step before the questionnaire — choice is remembered for next run. Run mode (Smart/Gaming/Safe) now auto-selected based on use case (FPS/Open-World → Gaming, Streaming/Mixed → Smart) and collapsed under a "Recommended" disclosure in the review step so power users can still override. Use-case-specific extra tweaks now injected into the build list (FPS: DPC Coalescing Off + Interrupt Steering, VRR/G-Sync Borderless, WASAPI 1ms Buffer; Open-World: adds DirectStorage + NVMe Queue Depth and SysMain disable on 16GB+ systems; Mixed: union of both). Gaming Readiness Score (0–100, animated count-up) added to the post-run impact report alongside RAM freed and DPC tiles — same formula as Smart Profile Wizard. Step indicator bar expanded from 4 to 5 steps. Back button added to Step 1 (questionnaire) to return to the goal selector. EN+FI translations added for all new strings.
v1.5.7 — Fix: pressing X (close button) now always fully quits the app — the will-quit cleanup handler (session webhook + Discord RPC teardown) now has a 3-second hard timeout via Promise.race, so if the async cleanup hangs (no network, Discord not running) the process exits anyway instead of staying alive silently in the background.
v1.5.7 — Fix: NOT ADMIN false-negative: checkAdmin() now calls _runPSRaw directly instead of going through the runPS semaphore, so it is never blocked behind slow metrics scripts. Previously, when collectMetrics() held all 4 PS slots (3 concurrent metric scripts every 3s), a checkAdmin call via api.isAdmin() would sit in the queue until its 4000ms timeout expired — returning empty output which evaluated to false regardless of actual elevation status. Also added a queue-wait timeout to runPS so queued commands now reject with a clear error instead of silently hanging indefinitely.
v1.5.7 — Fix: boot splash "Detecting system specs…" no longer hangs indefinitely — api.onThermalTick and api.onLhmStatus were called unconditionally inside init() but were missing from preload.js, causing a TypeError that silently crashed init() before getSystemInfo() was ever called; added both to preload.js wired to their respective IPC channels. Also removed leftover debug file write (fs.writeFileSync to C:\Users\Pete\admin-debug.txt) and fixed ghost setTimeout leak in animDonePromise.
v1.5.7 — Removed USB Power Guard tweak: it disabled USB selective suspend at the service level (Services\usb\DisableSelectiveSuspend), which could permanently break USB keyboard input across reboots. USB Suspend Off (Background section) already covers this need safely; USB Power Guard was redundant and dangerous.
v1.5.7 — Fix: StickyKeys Guard restore now correctly resets all three accessibility keys (StickyKeys, ToggleKeys, FilterKeys/Keyboard Response) to Windows defaults — previously only StickyKeys was restored, leaving ToggleKeys and FilterKeys hotkeys permanently disabled after a restore.
v1.5.7 — IPC handler response shape fixes: deep audit of all 46 newly-added handlers against their exact callers in index.html found 13 response shape mismatches that would cause runtime property-access failures. Fixed: pulse-prearm-scan now returns { thermal: { tier, suggestion } } for the Pulse thermal card; freeze-get-pids returns { frozenPids: number[] } tracked in-memory via NtSuspendProcess (replaces non-existent Suspend-Process cmdlet); freeze-process and defrost-process use inline C# NtSuspendProcess/NtResumeProcess via Add-Type and maintain _frozenPids set; cpu-get-topology returns isHybrid/is3DVCache/pCores/eCores/name from WMI; cpu-get-pin-status returns { active, state: { exe } } from in-memory _cpuPinnedExe; cpu-pin-game and cpu-unpin-game maintain _cpuPinnedExe and build correct affinity mask from logical core count; irq-get-devices includes irq number from MessageSignaledInterruptProperties registry and wraps coreCount in a single PS call; irq-apply-game-affinity returns coreCount alongside changed count; scan-phantom-apps returns array directly with folderPath/folderName/label fields (not nested phantoms object); bios-boot-chronicle returns { boots } array with totalMs/biosMs/mainPathMs/postBootMs/ts from Diagnostics-Performance events 100/101/109/110/200; bios-stability-check returns { signals: Array<{ ok, label }> } from kernel panic/WHEA/critical-error checks; bios-capsule-check diffs live SCEWIN output against saved snapshot and returns { drifts: Array<{ settingName, from, to, changedAt, external, token }> }; bios-get-profile returns { entries } directly (handles both flat-array and nested-entries formats); aria-get-session-coach returns { grade, trend, bottleneck, avgFps, prevAvgFps, elapsedMs, sessionCount } computed from _ariaInsights; prepare-ddu-reinstall detects GPU vendor/name/driverVersion via WMI; detect-missing-runtimes returns missing as string[] of ids (not object array); install-runtimes progress event sends { step, total, name }; ryzen-auto-tune returns { recommended: { ppt, tdc, edc, stapm, tctl, reason } } (read-then-recommend, no auto-apply); thermal-get-status returns { lastTick: { cpuTemp } } using _lastMetrics first with LHM fallback; ao-get-cpu-caps returns flat { [appId]: number } object; ao-fingerprint-drift-cause returns { updates: string[] } of human-readable update descriptions.
v1.5.7 — IPC audit & repair: deep forensic scan of all three IPC layers found 46 api.* methods called in index.html that were never exposed in preload.js, 2 channels wired in preload.js with no ipcMain.handle() in main.js, and a listener leak from offWindowsHealthProgress (and 4 sister off-methods) never being added to preload. All 46 missing preload entries added across 14 feature groups: Process Freeze/Defrost, CPU Affinity/IRQ Pinning, FPS HUD Settings, Game Shield/Update Guard, Game History/Custom Games, Thermal Status, App Optimizer Extended (cpu caps, drift cause), Cleanup Extended (regrets, fingerprints, phantom apps), BIOS Extended (boot chronicle, stability check, capsule snapshot/check, get-profile), ARIA Session Coach, Focus Assist/Self Priority/Update Intercept/Idle Suppression toggles, Runtime Installer (detect, prepare-DDU, install + progress event), Tweak Diff Snapshot, and Ryzen AutoTune. getSettings() aliased to load-settings (was throwing TypeError silently in 4 places). offWindowsHealthProgress, offPreflightProgress, offAutoOptiProgress, offDebloatProgress, offSpecProgress added to prevent listener stacking on modal reopen. Pulse prearm guard (pulse-prearm-scan, pulse-prearm-defer) and discord-set-tweaks handlers added to main.js. All new channels have real or stub ipcMain.handle() implementations so invoke calls no longer hang.
v1.5.7 — FiveM: Added warning to "Reduce LOD Draw Distance" tweak — alerts users that it can cause low-texture/washed-out visuals and to restore if texture quality issues appear. EN+FI translations added.
v1.5.7 — FPS Overlay accuracy fix: counter now shows true instantaneous FPS (computed from the single most recent PresentMon frame time) instead of an averaged value over 80 frames. Updates ~20 times per second driven directly by PresentMon stdout — no polling delay. Watchdog timer shows '—' within 1s when the game stops sending frames. Fullscreen overlay: setAlwaysOnTop relativeLevel raised to push above other always-on-top windows.
v1.5.7 — FPS Overlay dedicated settings window: "Configure ↗" button next to the toggle opens a compact dark panel with full control over number color (color picker + 5 preset swatches: white/green/cyan/orange/red), size (Small/Medium/Large/XL), and screen corner placement (Top-Left/Top-Right/Bottom-Left/Bottom-Right). All changes apply instantly to the live overlay. Settings persist across sessions. EN+FI translations added for the Configure button.
v1.5.7 — FPS Overlay redesign: minimal single white number (no labels, no 1% Low, no background panel), updates every 1 second for real-time accuracy (was 5s), white color with drop shadow for readability over any game background, font size now configurable from Settings (Small/Medium/Large/Extra Large) with live window resize — no restart required. EN+FI translations added for size setting.
v1.5.7 — Cleanup tab deep improvement: fixed 8 inaccurate size measurements — thumbnails now measures only thumbcache/iconcache DB files (not the entire Explorer folder), browser-history measures only the specific History+Cookies files deleted (not the whole Default profile, which was overstating by hundreds of MB), event logs measures actual .evtx files (was always 0), winsxs/fontcache/vs-temp/xbox/msstore were not measured at all and now return real values using PS globs and package lookups. Quick Clean modal can no longer be dismissed mid-run by clicking the backdrop — click-to-dismiss only activates after all jobs complete. Freed bytes now measured via actual disk-free delta (before vs after) instead of pre-scan totals that defaulted to 0 if Scan Sizes was never run; delta also stored as diskFreeBytes in cleanup history so the disk fill projection uses real growth rate (oldest vs newest snapshot) instead of the broken usedBytes/spanDays formula that projected near-term doom on healthy drives. Timeline auto-loads with a spinner placeholder instead of a blank div. History cards now show itemCount ("N items cleaned"). Disk Usage Analyzer bars color-coded by size (red ≥10 GB, yellow ≥1 GB, blue <1 GB) with a folder-open icon per row. Scan Sizes now shows a faint "· Clean" chip on items that are genuinely empty instead of hiding the chip (confirming the item was scanned). Language switch now instantly updates cleanup impact hints without requiring a page rebuild.
v1.5.7 — Fix: specs HUD (CPU/GPU/RAM/STORAGE chips) now visible on Dashboard tab; fix: sidebar nav pill no longer mispositiones when navigating away from Dashboard (was reading offsetTop before the 150ms HUD expand transition settled, causing a 32px jump).
v1.5.7 — Pulse tab comprehensive polish: Fixed 16 bugs and applied 23 UX improvements across all Pulse features. Bug fixes: borderBreathe CSS animation collision (chip-predict was overwriting card breathing animation); ARIA chip counter drift (replaced drifting integer counter with live DOM count); FPS spark right-aligned so newest data always fills right edge; FPS spark scale smoothed with exponential decay to prevent collapse on spikes; ELEVATED threat zone now correctly shows yellow (was identical green to OPTIMAL); threat label "SYSTEM THREAT" now tints with zone color at HIGH LOAD+; session debrief peak tile labels (CPU Peak, GPU Peak, RAM Peak, CPU Temp) fully translated EN+FI (were hardcoded English); FGD timeline now anchors to session start so events spread correctly across the full session width; FGD tooltip clamped to viewport bottom; AudioContext leaked on each activation (now module-level, reused); debrief auto-dismiss timer race condition on rapid restart fixed; Pre-Arm panel no longer stays open after Defer All & Launch; individual Defer button disables during API call; Thermal Audit has a 15-second cooldown with countdown label; contradictory green checkmark when throttlePct > 0 but estimatedFpsGain = 0 fixed; canvas sparks now defer draw with rAF when container is 0-width on first expand, and a ResizeObserver redraws them on window resize. UX improvements: RAM bar goes red/yellow at high usage (was always purple); CPU temp color-coded like GPU temp; per-core heatmap tooltip shows actual load %; monitor and FPS spark charts have gradient fill under lines; network metric replaced plain text with a progress bar (normalized to 10 MB/s); session history panel animates open/close, smoothness color-coded, hover state on header, open state persisted to localStorage; session debrief has a manual close button and adaptive timer (30s when BST/FGD present); BST and FGD panels slide in with cardIn animation; process table preserves scroll position on sort/filter, RAM bar cap is now dynamic (was fixed 512 MB), sort column shows ↑/↓ indicator, kill confirmation is two-click inline (no OS dialog), count shows "X / Y" when filter active; SF deviation banner: FPS improvement shown in green not warning yellow, added dismiss button; Pre-Arm panel list capped with internal scrollbar so no nested scroll, all-deferred state updates status text immediately; activation scan line crisp on HiDPI, card flash uses CSS class not inline style, audio chord changed to C major [C4/E4/G4]; FPS tiles show "FPS" unit suffix, stability tile shows "✓ smooth / some variance / choppy" hint; BST dots larger, verdict bolder; FGD overlapping events offset horizontally; thermal result uses CSS classes; active game hero shows game icon and pulsing live dot; history shows relative time ("2h ago"); ARIA chips suppress trivial <10% spikes and show wall-clock timestamp; threat meter diag text fades on zone change; all changes fully EN+FI translated.
v1.5.7 — UI/UX polish: removed card hover lift (cards no longer jump when hovering to reach controls inside), boosted card glow radial gradient opacity (now actually visible), page title size increased from 14px to 17px for clear hierarchy, sidebar section labels made more readable (larger font, no screaming caps), scrollbar visibility improved, dashboard stat tile labels fixed (were near-black-on-black), log pane now collapsed by default and auto-expands on section start or errors, language picker migrated from inline styles to CSS classes, confirm modal uses shared .modal-sm utility class.
v1.5.7 — Auto-Optimize + Smart Profile Wizard updated: 4 new tweaks added to Quick Wins strip (MMCSS Pro Audio, Keyboard Driver Queue, Defender Idle Priority, VRR/Borderless); 3 new tweaks added to Health Score recommended set; SPW_BASE gains 3 universal tweaks; SPW_FPS_EXTRA gains DPC Coalescing, VRR/Borderless, WASAPI 1ms; SPW_OPEN_EXTRA gains VRR/Borderless + DirectStorage; SPW_REVIEW_EXTRA gains GPU Preemption + Pool Usage Max; SPW_NVME_EXTRA gains DirectStorage. EN+FI Quick Win benefit strings added for all 4 new QW entries. 9 new high-impact performance tweaks: MMCSS Pro Audio Thread Scheduling (eliminates 1–5ms audio-induced micro-stutter in CS2/Valorant/EFT), DPC Coalescing Off + Interrupt Steering Off (removes up to 15ms DPC batch latency), GPU Preemption + Scheduler Tuning (2–8% better 1% lows), VRR/G-Sync in Borderless Windowed (eliminates 8–16ms vsync frame variance), Keyboard Driver Queue Tuning (mirrors mouse queue fix to kbdclass), Defender Process Priority Reduction (eliminates 50–200ms DPC spikes from background scans, adds CS2/Valorant/Apex/Fortnite process exclusions), Kernel Pool Usage Max 96% (reduces DX12/Vulkan command buffer stalls), DirectStorage + NVMe Queue Depth (raises queue from 32→64, enables DirectStorage hints), WASAPI Exclusive Mode 1ms Buffer (reduces audio engine callback latency from 10ms to 1ms). EN+FI translations throughout. Installer sidebar: removed baked-in version number (was never updated between releases). Metrics fixes: startup CPU/RAM query now uses -EA Stop with separate try/catch blocks so a WMI hiccup on one metric doesn't suppress the other; metrics error flag now resets after a successful collection so post-recovery errors are still reported; net speed calculation now uses actual elapsed time instead of a hardcoded 3 s divisor, fixing inflated readings when the poll interval is 10 s or 30 s.
v1.5.7 — Pulse: Update Guard (prevents Windows Update during game sessions), Game Shield (kills background processes on game launch), improved Pulse logic. Advanced: Interrupt Affinity Tool, CPU Core Topology Optimizer, Thermal Throttle Finder, Power Efficiency Monitor, Latency Profiler. App Optimizer: Smart Game Mode, Conflict Detector. Cleanup: Ghost App Detector, Disk Health Timeline. BIOS Tuner: Stability Confidence Score, Thermal Fingerprint, Boot Time Chronicle, improved logic. Settings: Quiet Mode, Low Resource Mode, ThermalGuard. Every tab audited and bugs fixed. ARIA analysis improved — significantly more accurate readings.
v1.5.7 — Premium access fix: bot redeploy (was down, causing offline fallback to grant cached premium to all users). Offline fallback now forces non-premium when bot is unreachable — users can still open the app, but premium features are locked until the bot confirms their role. LHM overlay fix: no longer shows error immediately when elevation is about to be attempted. LHM elevation fix: when WMI namespace is missing, LHM is now re-elevated via bundled elevate.exe (proper UAC prompt) instead of a silent Start-Process call that was suppressed or dismissed. Startup crash fix: reverted broken whole-app elevation relaunch that conflicted with the single-instance lock and caused immediate quit on launch.
