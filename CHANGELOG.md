v1.5.5 — LHM elevated relaunch: when WMI namespace is missing after retries (LHM running without admin rights), automatically kill and relaunch LHM via Start-Process -Verb RunAs (UAC prompt). Retries WMI probe after elevation; falls back to bot notification only if elevated relaunch also fails. UI shows "Relaunching LHM with elevated rights…" during the attempt.
