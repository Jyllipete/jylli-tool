v1.5.5 — LHM overlay fix: no longer shows error immediately when elevation is about to be attempted. Overlay stays blank until the elevation path resolves — shows "Relaunching LHM with elevated rights…" during the attempt, then either disappears (success) or shows the actual error (if elevation also fails).
